[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "department-telegram-assistant"
version = "0.1.0"
description = "Secure task assistant scaffold for Telegram and Microsoft 365"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
department-assistant = "assistant.__main__:main"

[tool.setuptools]
packages = ["assistant"]
